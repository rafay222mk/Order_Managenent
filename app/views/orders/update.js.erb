<% if @order.errors.any?  %>
    $("#errorarea").html("<%= escape_javascript render( 'error' , order: @order) %>");
<% else %>
    $("#userRow_<%= @order.id %>").replaceWith("<%= escape_javascript render( 'user_row' , order: @order) %>");
    $(".close-modal").click();
<% end %> 